[build-system]
requires = [ "setuptools>=61.0", "wheel",]
build-backend = "setuptools.build_meta"

[project]
name = "memos"
version = "0.31.0"
description = "A package for Pensieve (previously known as memos)"
readme = "README.pypi.md"
classifiers = [ "Programming Language :: Python :: 3", "License :: OSI Approved :: Apache Software License", "Operating System :: OS Independent",]
requires-python = ">=3.10"
dependencies = [
    # Web framework
    "fastapi",
    "uvicorn",
    "httpx",
    # Data validation
    "pydantic>=2.0",
    "pydantic-settings>=2.6",
    # Database
    "sqlalchemy>=2.0",
    "alembic",
    "sqlite_vec>=0.1.6",
    # CLI
    "typer>=0.13",
    # Image processing
    "opencv-python",
    "pillow",
    "piexif",
    "imagehash",
    "mss",
    # System utilities
    "watchdog",
    "psutil",
    "py-cpuinfo",
    "magika",
    # NLP
    "jieba",
    "tabulate",
    # Core dependency (required by opencv and other core features)
    "numpy",
    # Platform-specific: Windows screenshot and system interaction
    "pywin32; sys_platform == 'win32'",
    # Platform-specific: macOS screenshot and system interaction
    "pyobjc-core; sys_platform == 'darwin'",
    "pyobjc-framework-Quartz; sys_platform == 'darwin'",
    "ocrmac; sys_platform == 'darwin'",
    # Observability
    "logfire[sqlalchemy]",
    "logfire[fastapi]",
    # Local OCR support
    "onnxruntime",
    "rapidocr>=2.1.0",
    "openvino==2025.1.0; sys_platform == 'win32'",
    # PostgreSQL support
    "psycopg2-binary",
]
[[project.authors]]
name = "arkohut"

[project.license]
file = "LICENSE"

[project.optional-dependencies]
# Local ML model support (torch-based embedding + model downloading)
local = [
    "torch",
    "sentence-transformers",
    "timm",
    "einops",
    "modelscope",
]

[project.urls]
Homepage = "https://github.com/arkohut/pensieve"

[project.scripts]
memos = "memos.commands:app"
pen = "memos.commands:app"

[tool.setuptools.package-data]
"*" = [ "static/**/*",]
"memos.plugins.ocr" = [ "*.yaml", "models/*.onnx",]
memos = [ "simple_tokenizer/**/*", "default_config.yaml", "migrations/**/*",]

[tool.setuptools.packages.find]
include = [ "memos*",]
